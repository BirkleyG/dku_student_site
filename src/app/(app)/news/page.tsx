import { ComingSoon } from "@/components/shell/ComingSoon";

export default function NewsPage() {
  return (
    <ComingSoon
      eyebrow="DKU News"
      title="Straight from the Lilypad."
      description="We're wiring up a live sync with the Lilypad so every issue shows up here, plus a one-click newsletter signup."
    />
  );
}
